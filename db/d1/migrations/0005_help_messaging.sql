-- Migration 0005: help messaging tables (already in 0001 initial schema)
-- help_threads, help_messages, messaging_handler_grants — all in initial schema.
-- Kept for sequence numbering parity.
SELECT 1;
