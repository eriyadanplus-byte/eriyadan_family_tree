-- Migration 0003: v1.2 fields (already in 0001 initial schema)
-- is_stub, claimed_by_user_id, avatar_version, social fields, spouses table,
-- approval_scopes table — all included in initial D1 schema.
-- Kept for sequence numbering parity with MySQL migrations.
SELECT 1;
